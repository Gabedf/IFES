#include <stdio.h>

int main() {
    
    /* Declaração de variáveis */
    int x, y;

    /* Entrada de dados */
    printf("Variável x: ")
    scanf("%d", &x)
    printf("Variável y: ")
    scanf("%d", &y)

    /* Processamento de dados */
    x = x + y
    y = x - y
    x = 
}