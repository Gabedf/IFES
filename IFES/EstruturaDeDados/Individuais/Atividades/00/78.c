#include <stdio.h>
#include <stdlib.h>

int search(float m[max][max], int nLin, int nCol, float v, int *lin, int *col) {

}

int main() {
    int m;
    printf("Enter the numbers of rows and columns: ");
    scanf("%d", &m);

    float mat[m][m];
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < m; j++) {
            printf("Enter a value: ");
            scanf("%f", &mat[m][m]);
        }
    }
}